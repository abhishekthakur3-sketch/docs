---
title: Popups
description: Popups are overlay containers for contextual content, forms, or workflows that appear above the main interface without navigating away.
icon: "window-restore"
---

## Overview

Popups are overlay containers that display contextual content above the main interface. They consist of a Header, Content Container (slot-based), optional Snackbar, optional Tab Group, and Footer with action buttons. The system supports five sizes from XSmall (mobile) to XLarge (wide desktop) and includes dedicated header and footer sub-components.

<CardGroup cols={3}>
  <Card title="5 Sizes" icon="expand">
    XSmall (328px), Small (416px), Medium (632px), Large (848px), XLarge (1064px)
  </Card>
  <Card title="3 Header/Footer Sizes" icon="ruler">
    Large, Medium, Small
  </Card>
  <Card title="Slot-Based Content" icon="puzzle-piece">
    Flexible content area for custom components
  </Card>
</CardGroup>

---

{/* 🎨 DESIGNER: Replace with popup component preview from Figma */}
<div style={{background:'#F5F5F5', borderRadius:'12px', padding:'48px', marginBottom:'24px', textAlign:'center', minHeight:'200px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
  <div style={{opacity:0.4, fontSize:'14px'}}>🖼️ Popup Component Preview — Replace with /assets/components/popups/popup-preview.png</div>
</div>

---

## Playground

<Tabs>
  <Tab title="Header and Footer">
    <div style={{background:'#F5F5F5', borderRadius:'12px', padding:'32px', textAlign:'center', minHeight:'200px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{opacity:0.4, fontSize:'14px'}}>🖼️ Popup Header (Large, Medium, Small) and Footer (Large, Medium, Small)<br/>Replace with /assets/components/popups/popup-header-footer.png</div>
    </div>
  </Tab>
  <Tab title="All Sizes">
    <div style={{background:'#F5F5F5', borderRadius:'12px', padding:'32px', textAlign:'center', minHeight:'400px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{opacity:0.4, fontSize:'14px'}}>🖼️ Popup — XSmall, Small, Medium, Large, XLarge sizes<br/>Replace with /assets/components/popups/popup-sizes.png</div>
    </div>
  </Tab>
</Tabs>

---

## Sizes

| Size | Width | Usage |
|------|-------|-------|
| XSmall | 328px | Mobile popups, simple confirmations |
| Small | 416px | Compact forms, mobile-friendly content |
| Medium | 632px | Standard forms, moderate content |
| Large | 848px | Complex forms, data-heavy content |
| XLarge | 1064px | Wide layouts, multi-column content |

---

## Sub-Components

| Component | Variants | Description |
|-----------|----------|-------------|
| Popup Header | Large (82px), Medium (80px), Small (64px) | Title, subtitle, close button, optional actions |
| Popup Footer | Large (60px), Medium (60px), Small (56px) | Action buttons (primary, secondary, tertiary) |
| Content Container | Slot-based | Flexible area for custom content |
| Snackbar | Optional | Contextual information bar below header |
| Tab Group | Optional | Tab navigation within the popup |

---

## Anatomy

{/* 🎨 DESIGNER: Replace with popup anatomy from Figma */}
<div style={{background:'#F5F5F5', borderRadius:'12px', padding:'48px', marginBottom:'16px', textAlign:'center', minHeight:'200px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
  <div style={{opacity:0.4, fontSize:'14px'}}>🖼️ Popup Anatomy — Replace with /assets/components/popups/popup-anatomy.png</div>
</div>

| # | Element | Required | Description |
|---|---------|----------|-------------|
| 1 | Header | Yes | Title, subtitle, close button |
| 2 | Snackbar | No | Contextual info or warning bar |
| 3 | Tab Group | No | Tab navigation for sectioned content |
| 4 | Content Container | Yes | Slot for custom content |
| 5 | Footer | Yes | Action buttons |

---

## Behavior

| Interaction | Result |
|-------------|--------|
| Popup opens | Overlay backdrop appears, focus trapped inside |
| Click close button | Closes popup |
| Click primary action | Executes action, may close popup |
| Click secondary action | Cancels or alternative action |
| Click backdrop | Closes popup (if dismissible) |
| Press Escape | Closes popup |
| Tab key | Cycles focus within popup |
| Scroll | Content area scrolls independently |

---

## Usage Guidelines

<CardGroup cols={2}>
  <Card title="Do" icon="check" color="#16A34A">
    - Use popups for focused tasks that don't require full navigation
    - Choose the appropriate size for the content complexity
    - Include clear header titles and action labels
    - Use the slot system for custom content
    - Trap focus inside the popup while open
  </Card>
  <Card title="Don't" icon="xmark" color="#DC2626">
    - Don't use popups for simple confirmations (use Dialog Box)
    - Don't stack multiple popups
    - Don't use XLarge on mobile viewports
    - Don't remove the close/dismiss option
    - Don't overload popups with too much content
  </Card>
</CardGroup>

---

## Accessibility

| Requirement | Guideline |
|-------------|-----------|
| Role | Use `role="dialog"` with `aria-modal="true"` |
| Label | Provide `aria-labelledby` pointing to the header title |
| Focus trap | Focus must be trapped inside the popup while open |
| Keyboard | Escape closes, Tab cycles through elements |
| Screen reader | Announce popup title on open |
| Return focus | Return focus to trigger element on close |

---

## Design Tokens

<Tabs>
  <Tab title="Container">
    | Token | Value |
    |-------|-------|
    | `popup-width-xs` | 328px |
    | `popup-width-sm` | 416px |
    | `popup-width-md` | 632px |
    | `popup-width-lg` | 848px |
    | `popup-width-xl` | 1064px |
    | `popup-border-radius` | 16px |
    | `popup-shadow` | 0 8px 32px rgba(0,0,0,0.16) |
    | `popup-backdrop-bg` | rgba(0,0,0,0.5) |
  </Tab>
  <Tab title="Header/Footer">
    | Token | Value |
    |-------|-------|
    | `popup-header-height-lg` | 82px |
    | `popup-header-height-md` | 80px |
    | `popup-header-height-sm` | 64px |
    | `popup-footer-height-lg` | 60px |
    | `popup-footer-height-md` | 60px |
    | `popup-footer-height-sm` | 56px |
    | `popup-content-padding` | 16px |
  </Tab>
</Tabs>

---

## Developer Handoff

<CodeGroup>
```tsx API
interface PopupProps {
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
  open: boolean;
  onClose: () => void;
  title: string;
  subtitle?: string;
  snackbar?: ReactNode;
  tabs?: TabProps[];
  children: ReactNode;
  primaryAction?: { label: string; onClick: () => void };
  secondaryAction?: { label: string; onClick: () => void };
  tertiaryAction?: { label: string; onClick: () => void };
  dismissible?: boolean;
}
```

```tsx Usage
// Medium popup with form
<Popup
  size="md"
  open={showPopup}
  onClose={() => setShowPopup(false)}
  title="Create Order"
  subtitle="Fill in the order details"
  primaryAction={{ label: 'Submit', onClick: handleSubmit }}
  secondaryAction={{ label: 'Cancel', onClick: () => setShowPopup(false) }}
>
  <OrderForm />
</Popup>

// Large popup with tabs
<Popup
  size="lg"
  open={showDetails}
  onClose={() => setShowDetails(false)}
  title="Order Details"
  tabs={[
    { label: 'Summary', content: <Summary /> },
    { label: 'Timeline', content: <Timeline /> },
  ]}
  primaryAction={{ label: 'Close', onClick: () => setShowDetails(false) }}
>
  {activeTabContent}
</Popup>

// XSmall mobile popup
<Popup
  size="xs"
  open={showMobile}
  onClose={() => setShowMobile(false)}
  title="Confirm"
  primaryAction={{ label: 'OK', onClick: handleOk }}
>
  <p>Are you sure?</p>
</Popup>
```
</CodeGroup>
