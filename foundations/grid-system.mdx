---
title: Grid System
description: Layout grid framework, breakpoints, and responsive structure for the TARMAC Design System
icon: "grid"
---

## Overview

A grid system is the invisible structural framework that organizes layout, spacing, and alignment across your product. It ensures consistency, predictability, and rhythm — making interfaces feel balanced, intuitive, and easy to scan. A strong grid acts like a "traffic system" for UI elements: guiding where components should sit, how they scale, and how they respond across breakpoints.

<CardGroup cols={3}>
  <Card title="For Designers" icon="pen-ruler">
    Use Figma grid styles mapped to these specs. Align all elements to the defined column structure — never eyeball placement.
  </Card>
  <Card title="For Developers" icon="code">
    Reference grid tokens and breakpoint values. Build layouts using the 12-column (desktop) or 4-column (mobile) system with consistent gutters and margins.
  </Card>
  <Card title="For Product" icon="users">
    Ensures predictable, scannable layouts across all viewports — from compact POS terminals to wide enterprise dashboards.
  </Card>
</CardGroup>

## Grid Usage Guidelines

Choosing the right grid configuration depends on the content type, density, and user task. Use this decision framework to select the appropriate layout.

<Tabs>
  <Tab title="When to Use Fluid vs Fixed">
    | Scenario | Grid Type | Reason |
    |----------|-----------|--------|
    | Dashboards & analytics | Fluid | Maximize data visibility across screen widths |
    | Forms & data entry | Fixed (narrow) | Constrain width for comfortable reading and input |
    | Marketing / landing pages | Fixed (wide) | Control line length and visual composition |
    | Data tables & lists | Fluid | Tables need horizontal space to avoid truncation |
    | Settings / configuration | Fixed (narrow) | Focused, single-task workflows benefit from constraints |
    | Multi-panel workflows | Fluid | Side panels + content need proportional scaling |
  </Tab>
  <Tab title="Column Strategy by Context">
    | Context | Desktop Columns | Mobile Columns | Notes |
    |---------|----------------|----------------|-------|
    | Full-width dashboard | 12 | 4 | Cards span 3-4 cols each on desktop |
    | Form with sidebar | 8 (form) + 4 (sidebar) | 4 (stacked) | Sidebar collapses below form on mobile |
    | Data table | 12 | 4 (horizontal scroll) | Tables maintain structure, scroll horizontally |
    | Article / documentation | 8 (content) + 4 (TOC) | 4 (TOC hidden) | Table of contents hides on mobile |
    | Modal / dialog | 6-8 (centered) | 4 (full width) | Modals use a subset of the grid |
    | Login / auth screens | 4-6 (centered) | 4 | Narrow, focused layout |
  </Tab>
</Tabs>

---

## Grid Anatomy

Every TARMAC grid is composed of three core elements that work together to create structured, balanced layouts.

<div style={{background:'#F5F5F5',borderRadius:'12px',padding:'24px',marginBottom:'16px'}}>
  <div style={{display:'flex',alignItems:'stretch',height:'100px',border:'2px dashed #ED1B36',borderRadius:'8px',position:'relative'}}>
    <div style={{width:'24px',background:'rgba(237,27,54,0.08)',display:'flex',alignItems:'center',justifyContent:'center'}}>
      <div style={{fontSize:'9px',color:'#ED1B36',writingMode:'vertical-rl',transform:'rotate(180deg)'}}>MARGIN</div>
    </div>
    <div style={{flex:1,display:'flex',gap:'8px',padding:'8px 0'}}>
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
      <div style={{flex:1,background:'rgba(237,27,54,0.15)',borderRadius:'4px'}} />
    </div>
    <div style={{width:'24px',background:'rgba(237,27,54,0.08)',display:'flex',alignItems:'center',justifyContent:'center'}}>
      <div style={{fontSize:'9px',color:'#ED1B36',writingMode:'vertical-rl',transform:'rotate(180deg)'}}>MARGIN</div>
    </div>
  </div>
  <div style={{display:'flex',justifyContent:'center',gap:'32px',marginTop:'12px',fontSize:'12px'}}>
    <div style={{display:'flex',alignItems:'center',gap:'6px'}}><div style={{width:'12px',height:'12px',background:'rgba(237,27,54,0.15)',borderRadius:'2px'}} /> Columns</div>
    <div style={{display:'flex',alignItems:'center',gap:'6px'}}><div style={{width:'12px',height:'12px',background:'#F5F5F5',border:'1px solid #ccc',borderRadius:'2px'}} /> Gutters</div>
    <div style={{display:'flex',alignItems:'center',gap:'6px'}}><div style={{width:'12px',height:'12px',background:'rgba(237,27,54,0.08)',borderRadius:'2px'}} /> Margins</div>
  </div>
</div>

<CardGroup cols={3}>
  <Card title="Columns" icon="table-columns">
    Divide the page into equal vertical sections. Desktop uses 12 columns; mobile uses 4.
  </Card>
  <Card title="Gutters" icon="grip-lines-vertical">
    Gaps between columns. TARMAC uses 16px or 24px gutters depending on breakpoint.
  </Card>
  <Card title="Margins" icon="arrows-left-right">
    Outer edges of the grid preventing content from touching screen edges.
  </Card>
</CardGroup>

---

## Types of Grid

<Tabs>
  <Tab title="Fluid Grid">
    A fluid grid stretches across the screen to fill all available space. Columns and content scale and resize proportionally with the viewport.

    **When to use:**
    - Responsive layouts that need to adapt to any screen width
    - Content-heavy pages where maximizing reading area matters
    - Dashboards and data-dense interfaces

    <Info>Fluid grids are the default for most TARMAC layouts. They ensure content fills the available space while maintaining column proportions.</Info>
  </Tab>
  <Tab title="Fixed Grid">
    A fixed grid applies an ideal maximum width to page containers. Content is centered within the viewport, and the grid does not stretch beyond its defined max-width.

    **Variants:**
    - **Fixed Narrow** — For focused reading experiences and forms
    - **Fixed Wide** — For content-rich layouts with sidebars

    **When to use:**
    - Marketing and landing pages where line length control matters
    - Form-heavy workflows that benefit from constrained width
    - Screens where content readability is more important than space utilization

    <Tip>On viewports wider than 1440px, the TARMAC grid locks to a 1280px centered container to prevent eye-strain from overly long line lengths.</Tip>
  </Tab>
</Tabs>

## Column Span & Offset

<Tabs>
  <Tab title="Column Span">
    Column span defines how many columns a content block occupies. Spans range from 2 to 12 columns on desktop, allowing flexible layout compositions.

    <div style={{background:'#F5F5F5',borderRadius:'12px',padding:'20px',marginBottom:'16px'}}>
      <div style={{display:'flex',flexDirection:'column',gap:'8px'}}>
        <div style={{height:'32px',background:'rgba(237,27,54,0.2)',borderRadius:'4px',width:'100%'}} />
        <div style={{display:'flex',gap:'8px'}}>
          <div style={{height:'32px',background:'rgba(35,150,251,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(35,150,251,0.2)',borderRadius:'4px',flex:1}} />
        </div>
        <div style={{display:'flex',gap:'8px'}}>
          <div style={{height:'32px',background:'rgba(27,168,110,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(27,168,110,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(27,168,110,0.2)',borderRadius:'4px',flex:1}} />
        </div>
        <div style={{display:'flex',gap:'8px'}}>
          <div style={{height:'32px',background:'rgba(207,159,2,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(207,159,2,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(207,159,2,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(207,159,2,0.2)',borderRadius:'4px',flex:1}} />
        </div>
        <div style={{display:'flex',gap:'8px'}}>
          <div style={{height:'32px',background:'rgba(124,58,237,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(124,58,237,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(124,58,237,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(124,58,237,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(124,58,237,0.2)',borderRadius:'4px',flex:1}} />
          <div style={{height:'32px',background:'rgba(124,58,237,0.2)',borderRadius:'4px',flex:1}} />
        </div>
      </div>
      <div style={{display:'flex',justifyContent:'space-between',marginTop:'8px',fontSize:'11px',color:'#888'}}>
        <span>Span 12</span><span>Span 6 + 6</span><span>Span 4 + 4 + 4</span><span>Span 3 × 4</span><span>Span 2 × 6</span>
      </div>
    </div>

    | Span | Desktop Width (approx.) | Common Use |
    |------|------------------------|------------|
    | 2 cols | ~16% | Sidebar icons, small widgets |
    | 3 cols | ~25% | Side panels, navigation |
    | 4 cols | ~33% | Cards, form sections |
    | 6 cols | ~50% | Split layouts, two-column content |
    | 8 cols | ~66% | Main content with sidebar |
    | 12 cols | 100% | Full-width sections, heroes |
  </Tab>
  <Tab title="Offset">
    Offset adds empty space by skipping columns before a content block. This creates visual balance and asymmetric layouts without adding empty containers.

    <div style={{background:'#F5F5F5',borderRadius:'12px',padding:'20px',marginBottom:'16px'}}>
      <div style={{display:'flex',flexDirection:'column',gap:'8px'}}>
        <div style={{display:'flex',gap:'0'}}>
          <div style={{flex:2}} />
          <div style={{height:'32px',background:'rgba(237,27,54,0.2)',borderRadius:'4px',flex:10}} />
        </div>
        <div style={{display:'flex',gap:'0'}}>
          <div style={{flex:4}} />
          <div style={{height:'32px',background:'rgba(35,150,251,0.2)',borderRadius:'4px',flex:8}} />
        </div>
        <div style={{display:'flex',gap:'0'}}>
          <div style={{flex:6}} />
          <div style={{height:'32px',background:'rgba(27,168,110,0.2)',borderRadius:'4px',flex:6}} />
        </div>
        <div style={{display:'flex',gap:'0'}}>
          <div style={{flex:8}} />
          <div style={{height:'32px',background:'rgba(207,159,2,0.2)',borderRadius:'4px',flex:4}} />
        </div>
        <div style={{display:'flex',gap:'0'}}>
          <div style={{flex:10}} />
          <div style={{height:'32px',background:'rgba(124,58,237,0.2)',borderRadius:'4px',flex:2}} />
        </div>
      </div>
      <div style={{display:'flex',flexDirection:'column',gap:'2px',marginTop:'8px',fontSize:'11px',color:'#888'}}>
        <span>Span 10, Offset 2</span>
        <span>Span 8, Offset 4</span>
        <span>Span 6, Offset 6</span>
        <span>Span 4, Offset 8</span>
        <span>Span 2, Offset 10</span>
      </div>
    </div>

    **Use offsets to:**
    - Center content within the grid
    - Create visual breathing room
    - Align content blocks with other sections on the page
    - Build asymmetric but intentional layouts

    <Warning>Avoid excessive offsets that push content too far from the viewport edge. Users should not have to scan across large empty areas to find content.</Warning>
  </Tab>
</Tabs>

---

## Do's and Don'ts

<CardGroup cols={2}>
  <Card title="✓ Do" icon="check" color="#1BA86E">
    - Always align content to the column grid — no eyeballing
    - Use predefined gutter widths (16px or 24px) — never arbitrary gaps
    - Maintain consistent margins across all pages at the same breakpoint
    - Follow the column structure for all layout decisions
    - Use column span to control content width, not padding hacks
    - Test layouts at all defined breakpoints (1920, 1440, 1366, 1280, 390, 360, 320)
    - Let content stack naturally when columns collapse on mobile
    - Use offset to create intentional whitespace, not empty divs
  </Card>
  <Card title="✗ Don't" icon="xmark" color="#DC143C">
    - Don't place elements off-grid or between column boundaries
    - Don't use arbitrary spacing values (e.g., 13px, 7px, 22px)
    - Don't break grid alignment for "visual fit" — fix the content, not the grid
    - Don't overcrowd columns — if content doesn't fit, use more columns or simplify
    - Don't mix grid systems (e.g., 12-col in one section, 10-col in another)
    - Don't hardcode pixel widths — use column spans and grid tokens
    - Don't hide content on mobile just because it doesn't fit — restructure instead
    - Don't use negative margins to "fix" alignment issues
  </Card>
</CardGroup>

---

## General Principles

<CardGroup cols={2}>
  <Card title="Consistency over complexity" icon="equals">
    Apply the same visual logic across all surfaces. A predictable grid reduces cognitive load for both users and builders.
  </Card>
  <Card title="Content-first alignment" icon="align-left">
    Grids support meaning — they don't compete with it. Layout decisions should serve the content hierarchy, not override it.
  </Card>
  <Card title="Predictable navigation zones" icon="compass">
    Users should always know where to look. Consistent placement of navigation, content, and actions builds spatial memory.
  </Card>
  <Card title="Scalable spacing system" icon="ruler-combined">
    Built from TARMAC spacing tokens. Grid gutters, margins, and offsets reference the same scale used across all components.
  </Card>
  <Card title="Clarity at operational scale" icon="binoculars">
    Interfaces must remain readable in real-world environments — from compact POS terminals to wide enterprise dashboards.
  </Card>
  <Card title="Grid stability" icon="lock">
    Layouts should not jump, shift, or realign unexpectedly. Transitions between breakpoints and navigation states must feel smooth and intentional.
  </Card>
</CardGroup>

<Warning>Never break the grid to "make something fit." If content doesn't align to the grid, revisit the content structure or component sizing — not the grid itself.</Warning>

---

## Continue Reading

<CardGroup cols={2}>
  <Card title="Advanced Grid Usage" icon="table-columns" href="/foundations/grid-advanced">
    Layout contexts, responsive behavior, navigation grids, and component mapping.
  </Card>
  <Card title="Grid Implementation" icon="code" href="/foundations/grid-implementation">
    Token mapping, edge cases, CSS implementation, and breakpoint handling.
  </Card>
</CardGroup>
