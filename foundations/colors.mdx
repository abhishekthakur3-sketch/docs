---
title: Colors
description: Color system and palette for the TARMAC Design System
icon: "palette"
---

## Overview

Color in the Tarmac Design System is built around a small set of core colors, extended into semantic tokens for surfaces, text, icons, and borders. The system separates raw values from semantic intent across three layers: Core → Semantic → Usage.

The color system serves as the visual language of TARMAC — it communicates brand identity, establishes hierarchy, signals interactivity, and ensures accessibility across every product surface. It is the single source of truth for all color decisions, shared between design (Figma) and engineering (code tokens).

<CardGroup cols={3}>
  <Card title="For Designers" icon="pen-ruler">
    Use Figma color styles mapped 1:1 to these tokens. Never detach or override.
  </Card>
  <Card title="For Developers" icon="code">
    Reference semantic CSS variables or TypeScript token exports. Never hardcode hex values.
  </Card>
  <Card title="For Product" icon="users">
    Ensures brand consistency, accessibility compliance, and scalable theming across all products.
  </Card>
</CardGroup>

## Principles

<AccordionGroup>
  <Accordion title="Semantic-First Usage" icon="tag">
    Always reference colors by their semantic role (`surface.error`, `text.heading.primary`), never by their raw hue (`crimson-500`). This ensures meaning is preserved even if underlying values change.
  </Accordion>
  <Accordion title="Consistency Through Constraints" icon="lock">
    The palette is intentionally constrained. Every color in the system has a defined purpose. Avoid introducing ad-hoc colors — if a new need arises, extend the system at the token layer.
  </Accordion>
  <Accordion title="Accessibility by Default" icon="universal-access">
    Contrast ratios are enforced at the semantic layer. Body text meets WCAG AA (4.5:1), large text meets 3:1, and UI elements meet 3:1 against their backgrounds. Accessibility is structural, not manual.
  </Accordion>
  <Accordion title="Scalability" icon="arrows-up-down-left-right">
    Adding a new brand, theme, or product variant should only require changes at the core or semantic layer — never at the component level. The architecture supports light/dark modes and future brand extensions.
  </Accordion>
  <Accordion title="Neutral First" icon="circle">
    Neutrals form the foundation of every interface. Accent and status colors are used sparingly and purposefully for emphasis, feedback, and wayfinding.
  </Accordion>
</AccordionGroup>

## Color Architecture

The system is organized in three layers. Each layer builds on the one below it, creating a clear separation between raw values and their application in UI.

<Steps>
  <Step title="Layer 1: Core Colors">
    Fixed, raw color values representing the full spectrum. These are the palette — they define what colors exist. Core colors are never referenced directly in UI.
  </Step>
  <Step title="Layer 2: Semantic Tokens">
    Purpose-driven mappings that assign meaning to core colors. Tokens like `surface.error.default` or `text.heading.primary` describe intent, not hue. If a color changes, the semantic name stays stable.
  </Step>
  <Step title="Layer 3: Usage (Components)">
    Components consume semantic tokens. A Button uses `surface.accent.default` for its background and `text.heading.light.primary` for its label. Components never know about raw hex values.
  </Step>
</Steps>

<Warning>Core colors are palette-only. No component or screen should reference them directly. All UI usage must go through semantic tokens.</Warning>

---

## Usage Guidelines

### Do

<CardGroup cols={2}>
  <Card>
    <div style={{background:'#F0FDF4', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', gap:'12px', alignItems:'center'}}>
        <div style={{background:'#DCFCE7', borderRadius:'8px', padding:'12px 20px', border:'1px solid #BBF7D0'}}>
          <span style={{color:'#166534', fontSize:'14px'}}>surface.success.weakest</span>
        </div>
        <div style={{background:'#0D0D0D', borderRadius:'8px', padding:'12px 20px'}}>
          <span style={{color:'#FFFFFF', fontSize:'14px'}}>surface.accent.default</span>
        </div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#16A34A', fontSize:'18px'}}>✓</span>
      <span style={{fontSize:'14px'}}>Use semantic tokens for all color references</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Always reference colors by their semantic role like surface.info.default or text.error.primary — never by raw hex values.</div>
  </Card>
  <Card>
    <div style={{background:'#F0FDF4', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', flexDirection:'column', gap:'8px', alignItems:'stretch', width:'80%'}}>
        <div style={{background:'#F5F5F5', borderRadius:'6px', padding:'10px', textAlign:'left'}}>
          <span style={{color:'#0D0D0D', fontSize:'13px'}}>Heading text — neutral-900</span>
        </div>
        <div style={{background:'#F5F5F5', borderRadius:'6px', padding:'10px', textAlign:'left'}}>
          <span style={{color:'#454545', fontSize:'12px'}}>Body text — neutral-700</span>
        </div>
        <div style={{background:'#F5F5F5', borderRadius:'6px', padding:'10px', textAlign:'left'}}>
          <span style={{color:'#808080', fontSize:'11px'}}>Caption text — neutral-500</span>
        </div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#16A34A', fontSize:'18px'}}>✓</span>
      <span style={{fontSize:'14px'}}>Maintain visual hierarchy with neutrals</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Use neutrals for structure and hierarchy. Reserve accent and status colors for emphasis, feedback, and wayfinding.</div>
  </Card>
  <Card>
    <div style={{background:'#F0FDF4', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', gap:'16px', alignItems:'center'}}>
        <div style={{display:'flex', alignItems:'center', gap:'6px', background:'#FEF2F2', borderRadius:'6px', padding:'10px 16px', border:'1px solid #FECACA'}}>
          <span style={{color:'#DC2626', fontSize:'16px'}}>⚠</span>
          <span style={{color:'#991B1B', fontSize:'13px'}}>Error: Field required</span>
        </div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#16A34A', fontSize:'18px'}}>✓</span>
      <span style={{fontSize:'14px'}}>Pair color with icons and text</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Never rely on color alone to communicate state. Always add a secondary indicator like an icon, label, or pattern.</div>
  </Card>
  <Card>
    <div style={{background:'#F0FDF4', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', flexDirection:'column', gap:'6px', alignItems:'stretch', width:'80%'}}>
        <div style={{background:'#F0F8FF', borderRadius:'6px', padding:'8px 12px', border:'1px solid #BFDBFE'}}>
          <span style={{fontSize:'11px', color:'#1E40AF'}}>Weakest — backgrounds</span>
        </div>
        <div style={{background:'#93C5FD', borderRadius:'6px', padding:'8px 12px'}}>
          <span style={{fontSize:'11px', color:'#1E3A5F'}}>Mid — borders, dividers</span>
        </div>
        <div style={{background:'#2563EB', borderRadius:'6px', padding:'8px 12px'}}>
          <span style={{fontSize:'11px', color:'#FFFFFF'}}>Strong — text, icons</span>
        </div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#16A34A', fontSize:'18px'}}>✓</span>
      <span style={{fontSize:'14px'}}>Use tint scale intentionally</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Lightest tints for backgrounds, mid-range for borders, and saturated values for text and icons.</div>
  </Card>
</CardGroup>

### Don't

<CardGroup cols={2}>
  <Card>
    <div style={{background:'#FEF2F2', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', flexDirection:'column', gap:'8px', alignItems:'center'}}>
        <div style={{background:'#FFFFFF', borderRadius:'6px', padding:'10px 20px', border:'1px solid #E5E7EB'}}>
          <code style={{color:'#DC2626', fontSize:'13px'}}>color: #ED1B36;</code>
        </div>
        <div style={{fontSize:'11px', color:'#991B1B', textDecoration:'line-through'}}>Hardcoded hex value</div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#DC2626', fontSize:'18px'}}>✗</span>
      <span style={{fontSize:'14px'}}>Don't hardcode hex values in components</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Never reference core colors directly like #ED1B36. Always use semantic tokens so themes and updates propagate automatically.</div>
  </Card>
  <Card>
    <div style={{background:'#FEF2F2', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', alignItems:'center', gap:'8px'}}>
        <div style={{background:'#DCFCE7', borderRadius:'6px', padding:'10px 16px', border:'1px solid #BBF7D0'}}>
          <span style={{color:'#DC2626', fontSize:'13px'}}>Error text on success bg</span>
        </div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#DC2626', fontSize:'18px'}}>✗</span>
      <span style={{fontSize:'14px'}}>Don't mix semantic meanings</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Never combine conflicting semantic colors on a single element, like error text on a success background. It confuses the message.</div>
  </Card>
  <Card>
    <div style={{background:'#FEF2F2', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', flexDirection:'column', gap:'8px', alignItems:'center'}}>
        <div style={{background:'#7C3AED', borderRadius:'6px', padding:'10px 20px'}}>
          <span style={{color:'#FFFFFF', fontSize:'13px'}}>Custom purple button</span>
        </div>
        <div style={{fontSize:'11px', color:'#991B1B', textDecoration:'line-through'}}>Not in the token system</div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#DC2626', fontSize:'18px'}}>✗</span>
      <span style={{fontSize:'14px'}}>Don't introduce off-palette colors</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Don't add colors outside the palette without extending the token system. Ad-hoc colors break consistency and theming.</div>
  </Card>
  <Card>
    <div style={{background:'#FEF2F2', borderRadius:'8px', padding:'24px', marginBottom:'16px', textAlign:'center', minHeight:'140px', display:'flex', alignItems:'center', justifyContent:'center', width:'100%'}}>
      <div style={{display:'flex', gap:'16px', alignItems:'center'}}>
        <div style={{width:'40px', height:'40px', borderRadius:'50%', background:'#16A34A'}}></div>
        <div style={{width:'40px', height:'40px', borderRadius:'50%', background:'#DC2626'}}></div>
        <div style={{fontSize:'12px', color:'#991B1B'}}>Color only = no context</div>
      </div>
    </div>
    <div style={{display:'flex', alignItems:'center', gap:'8px', marginBottom:'8px'}}>
      <span style={{color:'#DC2626', fontSize:'18px'}}>✗</span>
      <span style={{fontSize:'14px'}}>Don't use color as the only indicator</span>
    </div>
    <div style={{fontSize:'13px', opacity:0.7}}>Color alone doesn't communicate state to all users. Always add text labels, icons, or patterns alongside color.</div>
  </Card>
</CardGroup>

---

## Continue Reading

<CardGroup cols={2}>
  <Card title="Color Palette Reference" icon="palette" href="/foundations/colors-palette">
    Complete core, neutral, alpha, and semantic color ramps with visual swatches.
  </Card>
  <Card title="Color Implementation" icon="code" href="/foundations/colors-implementation">
    State colors, token mapping, component mapping, theme strategy, and testing.
  </Card>
</CardGroup>
